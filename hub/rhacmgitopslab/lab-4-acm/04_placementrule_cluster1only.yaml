apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  labels:
    app: httpd
  name: clarksdale
  namespace: simple-app
spec:
  clusterSelector:
    matchLabels:
      clusterid: clarksdale
